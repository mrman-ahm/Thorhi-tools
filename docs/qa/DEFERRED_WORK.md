# Deferred Work Register

This file holds real but nonblocking work so foundation phases stay focused.
Correctness, security, data-integrity, and current-phase usability issues still
interrupt implementation immediately.

## Verification Environment

- Run the authored Playwright visual and accessibility suite in a normal local
  or CI browser environment. Chromium cannot start in the current sandbox
  because its process and listener setup is denied with `EPERM`.

## Toolchain

- Resolve the four existing ESLint warnings in the legacy discovery, frame
  evolution, search command, and signature scene modules when those surfaces
  enter active rebuild scope.
- Recheck the Node `url.parse` deprecation warning after the Next.js toolchain is
  upgraded. It originates in build tooling and does not currently change site
  behavior.

## Client And Production Inputs

- Add authentication and authorization before exposing the catalogue review
  workspace outside a controlled development environment.
- Replace the temporary transparent raster logo with approved original or
  vector brand artwork.
- Review generated motion assets for provenance and creative approval before
  any integration.
- Configure durable inquiry delivery, storage, retention, attachments, and
  privacy handling before production launch.
